server:
  port: 8082

spring:
  application:
    name: cursos
  data:
    mongo:
      db:
        authentication-database: admin
        host: mongo
        port: 27017
        username: root
        password: fiap-mba
        database: cursos
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher